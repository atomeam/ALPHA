/**
 * Victus Bridge - stub for @aether/bridge
 * The actual bridge logic lives in apps/backend
 */
export class VictusBridge {
  constructor(_config?: { runtimeUrl?: string }) {}
  
  async send() { return { success: false }; }
}