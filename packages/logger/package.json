{
  "name": "@alpha/logger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Structured NDJSON event logger for Alpha (cockpit + trust kernel audit trail).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
