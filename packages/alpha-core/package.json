{
  "name": "@alpha/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Alpha self-improving loop — Proposal types, Curator (default-deny), Applier (9 hardening rules).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@alpha/tsconfig": "workspace:*",
    "typescript": "~5.8.2",
    "vitest": "^2.1.9"
  }
}
