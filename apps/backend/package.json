{
  "name": "@alpha/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/server.ts",
    "lint": "eslint src",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'backend: no tests yet'"
  },
  "dependencies": {
    "@alpha/alpha-core": "workspace:*",
    "@alpha/logger": "workspace:*",
    "@alpha/mcp-core": "workspace:*",
    "@alpha/nexus-core": "workspace:*",
    "@alpha/permissions": "workspace:*",
    "@google/genai": "^1.29.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.21.0"
  }
}
