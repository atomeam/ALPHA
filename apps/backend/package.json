{
  "name": "@alpha/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Alpha backend HTTP surface on :8080.",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint .",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@alpha/alpha-core": "workspace:^",
    "@google/genai": "^1.29.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.21.0",
    "vitest": "^2.1.9"
  }
}
