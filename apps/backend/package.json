{
  "name": "@alpha/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Alpha backend — Node/Express on :8080. Health, prompt dispatch, integration routing.",
  "scripts": {
    "dev": "node --watch --import tsx src/server.ts",
    "start": "node --import tsx src/server.ts",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@alpha/core": "workspace:*",
    "@alpha/logger": "workspace:*",
    "@alpha/nexus-core": "workspace:*",
    "@google/genai": "^1.29.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@alpha/tsconfig": "workspace:*",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vitest": "^2.1.9"
  }
}
