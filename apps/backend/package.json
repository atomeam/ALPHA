{
  "name": "@alpha/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Alpha backend service on :8080.",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "dev": "tsx src/server.ts",
    "start": "tsx src/server.ts",
    "lint": "eslint .",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@alpha/alpha-core": "workspace:*",
    "@google/genai": "^1.29.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vitest": "^2.1.9"
  }
}
